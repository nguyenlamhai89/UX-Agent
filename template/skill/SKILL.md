---
name: Skill Name
description: A brief one-sentence summary of what this skill does for the orchestrator's routing.
---

# Skill Name

## Description

<!-- Replace with a detailed explanation covering: -->
<!-- 1. What this skill does and its core purpose -->
<!-- 2. When the orchestrator should trigger this skill (keywords, intent patterns, context) -->
<!-- 3. Any prerequisites or dependencies -->

Provide a detailed explanation of what this skill does, its core purpose, and the specific context or trigger conditions under which the orchestrator should execute it.

## Input

- **Type**: `dict`
- **Format**: Describe every field, its data type, and whether it is required or optional.
- **Location**: <!-- Pick one --> `query_param` | `request_body` | `file_path` — where the input is sourced from.
- **Input File(s)**:
  <!-- List every input file this skill reads, with its exact naming format. Use <placeholder> for dynamic parts. Remove this section if the skill does not read any files. -->
  - `<exact_filename_or_pattern>` — Description of the file and which skill produces it.
  - Example: `full-questionnaire.md` — The questionnaire table generated by the `create-questionnaire-table` skill.
  - Example: `transcript_<audio_name>.md` — Transcript files generated by the `transcribe-audios` skill.
- **Examples**:

  **Example 1** — Single text query:
  ```json
  {
    "query": "Find information about X"
  }
  ```

  **Example 2** — Query with optional parameters:
  ```json
  {
    "url": "https://example.com/data",
    "limit": 10
  }
  ```

## Output

- **Type**: `dict`
- **Format**: Describe every field in the response and its data type.
- **Location**: <!-- Pick one --> `query_param` | `request_body` | `file_path` — where the output is delivered to.
- **Output File(s)**:
  <!-- List every output file this skill produces, with its exact naming format. Use <placeholder> for dynamic parts. Remove this section if the skill does not produce any files. -->
  - `<exact_filename_or_pattern>` — Description of the file and its purpose.
  - Example: `full-questionnaire.md` — The structured questionnaire table.
  - Example: `mapped-transcript-<audio_name>.md` — Per-interviewee mapped transcript.
- **Examples**:

  **Example 1** — Single result:
  ```json
  {
    "status": "success",
    "result": "Here is the information about X..."
  }
  ```

  **Example 2** — List of results:
  ```json
  {
    "status": "success",
    "results": [
      { "id": 1, "title": "Item A" },
      { "id": 2, "title": "Item B" }
    ],
    "total": 2
  }
  ```

- **Specific format output (e.g., Markdown template)**:
  <!-- Provide a specific output format example here if the skill generates a specific format, such as a Markdown template. -->
  ```markdown
  # [Title]

  Formatted content...
  ```

## API Key

<!-- Remove this section entirely if the skill does not call any external API or LLM. -->

| Field | Value | Notes |
| --- | --- | --- |
| **Key** | `API_KEY_NAME` | Must match the variable name defined in the orchestrator's environment. |
| **Model** | `model-name` | Only required if the skill calls an LLM. |

> **Note**: Skills MUST NOT read API keys directly from the `.env` file. The orchestrator is responsible for reading the `.env` file and passing the necessary API key into this skill as an input parameter. Never hardcode keys in skill files.
>
> If the orchestrator does not provide an API key for this skill, **use the built-in Antigravity AI** to process the task instead.

## Custom Instructions

<!-- List every specific instruction / prompt that will be sent to the AI model when this skill executes. Each bullet should be a clear, actionable directive. Remove this comment block when filling in. -->

- Instruction 1 — Describe what the model should do (e.g., "Identify and label each speaker as Host, Interviewee, etc.").
- Instruction 2 — Describe formatting requirements (e.g., "Produce timestamps in HH:MM:SS format").
- Instruction 3 — Describe output constraints (e.g., "Return the result as valid Markdown").
- Clean up — Ensure that any temporary files created during processing (e.g., intermediate files, converted media files, safe upload copies) are deleted immediately after the skill completes its task.
- Add as many bullet points as needed to fully specify the model's behavior for this skill.

## Sequence Diagram

<!-- Draw Mermaid Diagram to show the full data flow of this skill, including the input and output data. Do not add any additional information to the diagram. The diagram should be easy to understand and follow. -->

```mermaid
sequenceDiagram
    autonumber
    actor User
    participant Orchestrator
    participant Skill

    User->>Orchestrator: Send request / query
    activate Orchestrator
    Note over Orchestrator: Parse intent,<br>select best Skill

    Orchestrator->>Skill: Execute (Input Data dict)
    activate Skill

    alt Success
        Skill->>Orchestrator: Return success status and result dict
        deactivate Skill
        Orchestrator->>User: Respond with formatted result
        deactivate Orchestrator
    else Failure
        Skill->>Orchestrator: Return error status and error message
        deactivate Skill
        Orchestrator->>User: Respond with fallback / error message
        deactivate Orchestrator
    end
```

## Error Handling & Fallbacks

<!-- Replace the rows below with the actual errors this skill can produce. -->

| Error Code | Message | Fallback Behavior |
| --- | --- | --- |
| `INVALID_INPUT` | The input is missing required fields or has an invalid format. | Return a clear validation error to the user. |
| `API_ERROR` | The external API returned an unexpected error. | Retry once, then return a friendly error message. |
| `TIMEOUT` | The request timed out before completing. | Retry once, then notify the user to try again later. |

## Known Bugs & Resolutions

<!-- List any past bugs or errors encountered during the development or execution of this skill, and explain how they were resolved. This acts as a knowledge base for future maintenance. -->
> **Agent Rule (Error Handling & Bug Documentation):** In the future, when this skill encounters an error during input receiving, processing, or output generation, the AI agent must first propose a solution to the user. If the user agrees, the AI agent will fix the error. If the error is successfully fixed, the AI agent must update this 'Known Bugs & Resolutions' section with the bug, cause, and resolution.

| Bug / Error | Cause | Resolution |
| --- | --- | --- |
| `Bug Example` | Why did it happen? | How was it fixed? |

## Performance Improvement Solutions

<!-- This section is automatically populated and updated by the analyze-skill. It contains a checklist of actionable solutions to improve this skill's performance across various criteria. -->