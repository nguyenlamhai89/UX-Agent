# Skill Performance Analysis: visualize-insights

> Last updated: 2026-07-22

---

## 1. ⚡ Execution Efficiency

| Criteria | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-14 | 2026-07-14 | 2026-07-16 | 2026-07-22 |
|----------|---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ----------|
| Execution Time | • **7/10** — Relies on AI generation which can take 10-30s. | • **8/10** — Execution is fast due to deterministic local parsing. External subprocessing (ffprobe) is synchronous but acceptable for small scale. | • **9/10** — Fast local string parsing. Generates output almost instantly. | • **9/10** — Execution is fast — purely deterministic local string parsing with no external API calls. The only external process is `ffprobe` for audio duration (called per file with a 5s timeout). For typical UX research projects (1-10 audio files), this completes in under 1 second. | • **9/10** — Performs deterministic local Python parsing and assemblies, running in sub-second times. | • **9/10** — Performs deterministic local Python parsing and assemblies, running in sub-second times. | • **9/10** — Runs in sub-second times using deterministic local Python parsing and HTML string assemblies. | • **9/10** — Deterministic local parsing is linear and ffprobe duration probes run concurrently, so typical research datasets complete quickly. |
| API Call Count | • **9/10** — Only 1 call to Built-in AI per request. | • **10/10** — 0 API calls are made for core generation, relying entirely on local Python logic. | • **10/10** — No external APIs used, 0 calls. | • **10/10** — Zero external API calls. All generation is local Python parsing. The skill previously used Built-in AI but was refactored to fully deterministic logic. | • **10/10** — Requires exactly zero API calls during execution, eliminating remote failures and token costs. | • **10/10** — Requires exactly zero API calls during execution, eliminating remote failures and token costs. | • **10/10** — Zero external API calls are made for core generation, relying entirely on local Python logic. | • **10/10** — Runtime generation makes no external API calls; browser assets are fetched only when the produced report is viewed. |
| Token Usage | • **6/10** — Passes two potentially large files (insights.md and mapped-transcript.md), which could consume a lot of tokens. | • **10/10** — No tokens used. | • **10/10** — No LLM token usage. | • **10/10** — No LLM token usage since the refactor to deterministic local parsing. All HTML is generated via string manipulation and template injection. | • **10/10** — Consumes zero LLM tokens at runtime by using deterministic local string parsing. | • **10/10** — Consumes zero LLM tokens at runtime by using deterministic local string parsing. | • **10/10** — Consumes zero LLM tokens at runtime by using deterministic local string parsing. | • **10/10** — The runtime uses no LLM and therefore consumes no model tokens. |
| Resource Consumption | • **9/10** — Minimal local resource usage (just read/write files). | • **9/10** — Reads files directly into memory which is fine for current scale. | • **9/10** — Memory efficient since text is not extremely large. | • **9/10** — Reads files into memory entirely, which is fine for the typical scale (insights.md ~6KB, mapped-transcript.md ~20KB). No temporary files are created. Output is a single HTML file. | • **9/10** — Uses minimal memory to parse Markdown lines and writes a single static HTML file. | • **9/10** — Uses minimal memory to parse Markdown lines and writes a single static HTML file. | • **9/10** — Reads files directly into memory, which is highly efficient for typical research project scales. Writes a single static HTML file. | • **7/10** — No temporary files are created, but every source and every full transcript is loaded and embedded into one in-memory HTML document with no size guard. • **Solution**: Implemented a configurable 50 MB text-input budget, an 80% warning threshold, large-study warnings, inert transcript JSON payloads, and on-demand drawer hydration. |

---

## 2. 🎯 Output Quality & Accuracy

| Criteria | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-14 | 2026-07-14 | 2026-07-16 | 2026-07-22 |
|----------|---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ----------|
| Output Completeness | • **6/10** — Generates a full HTML file but there is a risk of AI omitting parts of the template or hallucinating structure. | • **8/10** — Data bindings work well, but we must ensure that the final HTML exactly binds all contents by enforcing strict parsing regex for the Markdown tables to prevent lost data on malformed inputs. | • **9/10** — Successfully covers all tables and dynamically handles N columns. | • **6/10** — CRITICAL DISPLAY ISSUE: `parse_insights_to_html()` parses the per-interviewee Insights table (columns: # | Insight | Quotes) but the Data Saturation Matrix section (columns: Insight | User1 | User2...) has a completely different schema. The function only finds the FIRST table with 'Insight' in header and uses its column layout for ALL subsequent tables. With 1 interviewee, the saturation matrix header 'Quotes' is displayed instead of the actual user name ('Chị Dung'). Additionally, when `audio_names` is available from `get_folder_stats()`, the header replacement logic at lines 179-184 correctly overrides, but only if audio files exist in the output directory. If the output_dir differs from the audio source dir, `total_interviewees` will be 0 and `audio_names` will be empty, causing fallback to the markdown headers which may be wrong ('Quotes' instead of user names). | • **9/10** — Correctly parses and populates stats, saturation matrices, quote lookups, and full transcript tables. | • **10/10** — Correctly parses and populates stats, matrices, quotes, and transcript tables, now complete with interactive transcript searching and highlighting. | • **10/10** — Successfully parses and populates all transcript tables, insights matrices, quote lookup links, and journey maps, resolving variable column headers issues. | • **7/10** — Valid canonical inputs produce overview, saturation, transcript drawer, and optional journey content, but empty parse results and missing required project_name can still return success. • **Solution**: Required project_name and absolute paths, rejected empty interviewee, saturation, transcript, and journey parses, and blocked unresolved placeholders before output. |
| Format Compliance | • **6/10** — HTML must adhere to Tailwind CSS. AI might hallucinate classes or miss specific styling requirements. | • **9/10** — HTML conforms to Ant Design/Tailwind specification. | • **9/10** — Outputs valid HTML using the agreed Ant Design template. | • **8/10** — The generated HTML correctly uses the Ant Design/Tailwind template structure. The modular template system (insights-template.html + 3 module files) is well-organized. All CSS classes conform to the Tailwind specification. The `toggleQuote()` JavaScript function correctly uses unique IDs (insight-1, insight-2, etc.). | • **9/10** — Enforces formatting via deterministic modules and converts markdown styles (like bold tags) to HTML matching definitions. | • **9/10** — Enforces formatting via deterministic modules and converts markdown styles (like bold tags) to HTML matching definitions. | • **10/10** — Strictly conforms to the Ant Design/Tailwind specification. Stage Emotion cells use white backgrounds, premium borders, and shadows without overriding bg-white. | • **6/10** — The template follows the expected visual family, but it retains Persona and Journey Map Soon badges contrary to workspace navigation rules and references a nonexistent saturation.html before falling back. • **Solution**: Renamed the canonical module to saturation.html, preserved Overview, Insights, Persona, and Journey Map navigation order, and removed all Soon UI. |
| Content Accuracy | • **7/10** — Depends entirely on AI accurately extracting data from markdown. | • **10/10** — 100% accurate since it deterministically copies exact markdown content into HTML without LLM hallucination. | • **10/10** — Accurate deterministic conversion from MD to HTML. | • **5/10** — MAJOR DISPLAY BUG: The insights table is being populated from the WRONG section of `insights.md`. The function `parse_insights_to_html()` parses the per-interviewee section `# Insights — Chị Dung` (which has columns `# | Insight | Quotes`) instead of the `# Data Saturation Matrix` section (which has columns `Insight | User1 | User2 | ...`). This means: (1) The saturation matrix view shows individual quotes instead of 🔵/🌐/⚪️ saturation markers. (2) The column headers show 'Quotes' instead of interviewee names. (3) Multi-interviewee saturation data is completely lost since only the first interviewee's insight table is parsed. Additionally, the `extract_chart_data()` function correctly parses the `**🔵 New Insights**` row, but the labels fallback to 'User 1', 'User 2' when `audio_names` is not provided, instead of using actual names from the markdown headers. | • **9/10** — Verbatim transcript contents, timestamps, and quotes are faithfully kept without changes or omissions. | • **9/10** — Verbatim transcript contents, timestamps, and quotes are faithfully kept without changes or omissions. | • **10/10** — 100% accurate since it deterministically copies exact markdown content into HTML without LLM hallucination. | • **5/10** — The chart labels data as cumulative while plotting per-interviewee new-insight counts and derives total insights with max instead of a cumulative sum; several mapped fields are also inserted without HTML escaping. • **Solution**: Converted new-insight counts to a cumulative series and escaped Markdown fields, project labels, safe links, headers, quotes, and embedded JSON. |
| Human Approval Rate | • **8/10** — No known bugs related to this, but users might often want visual tweaks. | • **9/10** — Consistent UI generation should result in high approval. | • **9/10** — Clean, highly readable UI. | • **6/10** — The Known Bugs section documents one prior bug (empty insight table due to split/index misalignment), which was fixed. However, the current display issues (wrong table being parsed, incorrect column headers) would likely cause user rejection when the saturation matrix tab shows quotes instead of saturation indicators. The transcript tab appears to work correctly with real data. The overview cards correctly show interviewee count, interview duration, and total insights. | • **9/10** — Highly polished Tailwind and Ant-Design themed layouts produce dashboard visual quality. | • **10/10** — Highly polished Tailwind and Ant-Design themed layouts produce dashboard visual quality, newly enhanced with a fully interactive transcript search bar and clear button. | • **10/10** — Robust parsing for variable column headers (Theme vs Topic) and proper visual component styling ensure immediate user acceptance. | • **7/10** — Four historical display regressions are documented and the 23 current tests pass, but there is no successful full-generation or visual regression test for navigation, responsive layout, and interactive states. • **Solution**: Added a representative full-pipeline fixture plus structural, interaction, accessibility, responsive, drawer, search, and empty-state assertions; live local-file browser QA was attempted but blocked by browser policy. |

---

## 3. 🔗 Workflow Fit

| Criteria | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-14 | 2026-07-14 | 2026-07-16 | 2026-07-22 |
|----------|---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ----------|
| I/O Contract Adherence | • **9/10** — Takes inputs matching the output of previous skills. | • **9/10** — Respects inputs/outputs defined in SKILL.md. | • **9/10** — Reads exactly what upstream produces. | • **8/10** — The input contract matches the upstream `saturate-insights` skill output (insights.md, mapped-transcript.md). The output contract (status + output_file path) is correct. However, the skill assumes `output_dir` is the same directory containing audio files (for `get_folder_stats()`), which may not always be true — the orchestrator might pass a different output directory than the audio source directory. | • **9/10** — Explicit paths for insights and mapped transcripts match what previous skills output. | • **9/10** — Explicit paths for insights and mapped transcripts match what previous skills output. | • **10/10** — Explicit paths for insights, mapped transcripts, and customer journey maps match upstream inputs perfectly. | • **7/10** — The declared inputs align with artifacts from ux-transcribe and ux-cjm, but no parent workflow currently encodes the full cross-workflow handoff; required project_name is optional in code and output_file is not normalized to the promised absolute path. • **Solution**: Added the ux-research parent workflow for ux-transcribe to ux-cjm to visualize-insights handoffs, required canonical absolute paths, and returned absolute HTML and manifest paths. |
| Skip-Logic Compatibility | • **9/10** — Output is deterministically named <project_name>.html. Easy to verify. | • **9/10** — Output file acts as clear marker for idempotency/skip-logic. | • **9/10** — Generates a final HTML file that orchestrator can track. | • **9/10** — Output is deterministically named `<project_name>.html`. The orchestrator can easily check for the existence of this file to determine if the skill has already run. | • **9/10** — Outputs checkable files, facilitating orchestrator skip checks on double execution. | • **9/10** — Outputs checkable files, facilitating orchestrator skip checks on double execution. | • **10/10** — Outputs checkable files, facilitating orchestrator skip checks on double execution. | • **4/10** — The intended parent chain has no freshness manifest or cross-stage skip rule, so the terminal visualization rebuilds and opens even when all upstream artifacts are unchanged. • **Solution**: Added a versioned input-signature manifest covering canonical text inputs, templates, media metadata, renderer version, and output hash, with deterministic skipped results. |
| Pipeline Passthrough Rate | • **6/10** — Error table mentions retrying for AI failure, but no retry logic is actually implemented in the script. | • **9/10** — Gracefully handles most standard inputs. | • **9/10** — Very stable, unlikely to halt pipeline. | • **9/10** — Parsing is robust — malformed markdown rows are silently skipped via `cols[0].isdigit()` check. File read errors are caught and returned as JSON errors. The skill is unlikely to halt the pipeline. | • **9/10** — Standard JSON error formats on stdout help calling orchestrators track execution status. | • **9/10** — Standard JSON error formats on stdout help calling orchestrators track execution status. | • **10/10** — Highly stable parsing; returns standard JSON error formats on stdout to aid orchestrator tracking. | • **6/10** — Some validation failures return structured codes, but generic reads and writes omit stable codes, journey failures degrade silently, and template-read fallbacks can leave variables undefined. • **Solution**: Replaced fallback template loading with one guarded loader and documented stable codes for input, parse, journey, template, path, write, browser, and internal failures in the parent workflow. |
| Idempotency | • **9/10** — Overwrites the HTML file at the same path. | • **10/10** — Generates identical HTML file on repeated runs. | • **10/10** — Overwrites HTML cleanly, perfectly idempotent. | • **10/10** — Running the skill twice produces identical output. The HTML file is overwritten at the same path. No side effects (no API uploads, no state mutation). | • **9/10** — Overwrites output files safely on multiple runs without side effects. | • **9/10** — Overwrites output files safely on multiple runs without side effects. | • **10/10** — Perfectly idempotent; repeated executions cleanly overwrite output files without side effects. | • **7/10** — The HTML file is overwritten deterministically for stable inputs, but automatic browser opening is an unconditional side effect and adjacent audio discovery can change output independently of declared inputs. • **Solution**: Made browser opening opt-in, replaced output-directory media discovery with explicit media_paths, and verified repeat runs against the manifest before rebuilding. |

---

## 4. 🛡️ Reliability & Error Handling

| Criteria | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-14 | 2026-07-14 | 2026-07-16 | 2026-07-22 |
|----------|---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ----------|
| Error Rate | • **7/10** — Python script handles missing inputs, but Agent.chat could throw unhandled network exceptions. Caught broadly. | • **9/10** — Parsing logic is stable, though highly dependent on upstream markdown formatting. | • **9/10** — Parsing is robust even if data is slightly malformed. | • **8/10** — Error handling covers missing input JSON, invalid JSON, missing required fields, file read failures, template read failures, and output write failures. All errors return structured JSON. No unhandled exceptions in normal operation. | • **9/10** — Catches file reading, JSON decoding, and output directory writing errors cleanly. | • **9/10** — Catches file reading, JSON decoding, and output directory writing errors cleanly. | • **9/10** — Catches file reading, JSON decoding, template reading, and output writing errors cleanly. | • **6/10** — Core helpers are tested, but duplicated exception branches make template failures unsafe, broad catches hide causes, project_name can escape output_dir, and malformed Markdown can succeed with empty content. • **Solution**: Refactored main into payload validation, template loading, signature, render, validation, publication, and response stages; constrained filenames and normalized unexpected errors. |
| Error Recoverability | • **8/10** — Fails gracefully returning JSON error. No temp files to clean up. | • **9/10** — Fails cleanly with stack trace if markdown is completely broken. | • **8/10** — Catches file read errors and exits cleanly. | • **8/10** — Fails cleanly with JSON error messages. No temporary files to clean up. The skill can be safely re-run after fixing the input issue. | • **9/10** — Returns clean non-zero codes on failure, helping the pipeline fail-fast and fail-safely. | • **9/10** — Returns clean non-zero codes on failure, helping the pipeline fail-fast and fail-safely. | • **9/10** — Returns clean non-zero codes on failure, helping the pipeline fail-fast and fail-safely without trailing state. | • **6/10** — Failures create no temporary residue, but direct writes can replace a good report non-atomically and optional journey errors only print a warning while returning success. • **Solution**: Published HTML and manifest through sibling temporary files with atomic replacement, removed failed temporary files, preserved last-known-good output, and made provided journey failures terminal. |
| Retry Success Rate | • **5/10** — No retries are implemented in the code for transient AI errors, despite what SKILL.md error table says. | • **10/10** — No transient network issues to retry. | • **9/10** — Fails fast, reruns will succeed if input is fixed. | • **9/10** — No transient failures possible since there are no network calls. Any failure is deterministic (missing files, malformed input). Re-running with the same input will produce the same result. | • **9/10** — Deterministic operations mean retries are not required; failures are terminal and predictable. | • **9/10** — Deterministic operations mean retries are not required; failures are terminal and predictable. | • **10/10** — No transient network failures possible since there are no API calls. Retry success is deterministic. | • **10/10** — Generation has no transient network or paid API dependency, so deterministic failures should be corrected rather than retried. |
| Known Bug Recurrence | • **9/10** — None yet. | • **10/10** — No recurring bugs documented. | • **9/10** — Recent bug was fixed properly, unlikely to recur. | • **7/10** — One documented bug (Insight Table Empty) was fixed with a targeted code change. However, the unit tests that should prevent regression are FAILING (3 out of 5 tests fail). The `test_parse_transcript_to_html` and `test_parse_insights_to_html` tests fail because they expect a string return value but the functions now return tuples. The `test_get_folder_stats` test fails because it passes 2 arguments to a function that only takes 1. These broken tests mean regressions would not be caught. | • **9/10** — Historic matrix and chart label alignment bugs have been resolved, and test cases have been added to prevent regressions. | • **9/10** — Historic bugs have been resolved, and test cases have been added to prevent regressions. | • **10/10** — Matrix parsing, chart label misalignment, and empty table bugs have been resolved, and test cases have been added to prevent regressions. | • **7/10** — Historical parser regressions have targeted tests, but success-path orchestration, template failures, cumulative chart math, atomic output, and navigation rules remain untested. • **Solution**: Added regressions for CLI success and errors, cumulative totals, injection safety, path containment, template placeholders, atomic failure recovery, manifest freshness, navigation, and the full parent pipeline. |

---

## 5. 💰 Cost & Scalability

| Criteria | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-13 | 2026-07-14 | 2026-07-14 | 2026-07-16 | 2026-07-22 |
|----------|---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ---------- | ----------|
| Cost per Execution | • **6/10** — 1 prompt to AI with potentially huge token counts. | • **10/10** — Free to run (local execution). | • **10/10** — Zero cost. | • **10/10** — Zero cost — fully local execution with no paid API calls. | • **10/10** — Completely free runtime execution due to local processing without API usage. | • **10/10** — Completely free runtime execution due to local processing without API usage. | • **10/10** — Completely free runtime execution due to local processing without API usage. | • **10/10** — Generation is local and has no paid API or token cost. |
| Scaling Behavior | • **5/10** — Large transcript = giant prompt = possible context window limit hit. | • **9/10** — Scales linearly with the number of rows. | • **7/10** — For 1 user, table-fixed with small min-w might leave empty space. For 10+ users, horizontal scrolling works, but UI density could be optimized. • **Solution**: Changed table-fixed to table-auto for better 1-user scaling. | • **8/10** — Scales linearly with input size. String parsing is O(n) where n is the number of lines. For typical UX research projects (5-20 interviewees, 50-200 transcript rows), performance is excellent. The only concern is with very wide tables (many interviewees) which might cause horizontal scroll UX issues, but the template already handles this with `overflow-x-auto` and `min-w-[300px]` per column. | • **9/10** — Processing times scale linearly with number of interviewees and questions. | • **9/10** — Processing times scale linearly with number of interviewees and questions. | • **9/10** — Processing times scale linearly with number of interviewees and questions. Table design supports responsive scrolling for wide matrix views. | • **6/10** — Parsing is linear, but all transcripts are embedded as live hidden DOM, transcript search walks text nodes, and wide matrices grow with every participant. • **Solution**: Hydrated only the active transcript drawer from inert payloads, added input and 20-participant warnings, and verified 20-user runtime and memory bounds. |
| Unit Test Coverage & Pass Rate | • **4/10** — tests directory and test files do not currently exist. | • **4/10** — The existing test suite (test_visualize_insights.py) still tests the old AI-based logic (mocking Agent) instead of the new deterministic functions (get_folder_stats, extract_summary_insights). | • **9/10** — E2E tests pass for the whole workflow including this skill. | • **3/10** — CRITICAL: 3 out of 5 unit tests are FAILING. (1) `test_parse_transcript_to_html` fails because the function now returns a tuple (rows, thead) but the test expects a single string. (2) `test_parse_insights_to_html` fails for the same reason — returns a 3-tuple (rows, thead, headers). (3) `test_get_folder_stats` fails because it passes 2 positional arguments but the function signature only accepts 1 (`output_dir`). Only `test_extract_summary_insights` and `test_main_missing_args` pass. No tests exist for `extract_chart_data()`, `parse_transcript_to_html()` with real multi-column data, or the full template assembly pipeline. | • **10/10** — Highly detailed unit test suite (12/12 passing) covers mapping stats, charts, single-user and multi-user modes. | • **10/10** — Highly detailed unit test suite (12/12 passing) covers mapping stats, charts, single-user and multi-user modes. | • **10/10** — Extensive unit test suite (14/14 passing) covers stats, charts, single-user and multi-user modes, header variations, and CJM emotion formatting. | • **7/10** — All 23 target tests pass, with good helper coverage, but the only main test is the missing-input case and measured coverage was unavailable because the coverage package is not installed. • **Solution**: Expanded the visualization suite to 40 passing tests and added a new end-to-end parent workflow test while preserving all existing workflow and skill suites. |
